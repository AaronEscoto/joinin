<%= simple_form_for @event do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :location, collection: @locations, as: :grouped_select, group_method: :titles %> <%# doesn't work yet %>
  <%= f.button :submit %>
<% end %>